<nav class="navbar navbar-light bg-light">

	<%=  link_to '* Test Guru *', root_path, class: 'navbar-brand' %>

	<span class="navbar-text">
		<% if user_signed_in? %>
			 <span class = "greet user">
				<%= t('.greet') %> <%=current_user.identity_name %>
				<%= current_user.identity_sname %>!&nbsp;
			 </span>
      <%= link_to t('.show_badge_link'), badges_path, \
																			 class: 'btn btn-outline-primary' %>
			<%= link_to t('.logout_link'), destroy_user_session_path, method: 'delete', \
																		 class: 'btn btn-outline-danger' %>
		<% else %>
			<%= link_to t('.login_link'), new_session_path(resource_name), \
																		class: 'btn btn-outline-info' %>
			<%= link_to t('.signup_link'), new_registration_path(resource_name), \
																		 class: 'btn btn-outline-warning' %>
    <% end %>
	</span>
</nav>
