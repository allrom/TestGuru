<div class="container">

	<h5><%= t('.header', count: @test.questions.count) %></h5>

	<table class="table table-condensed table-striped">
		<tr>
			<th><%= t('.id') %></th>
			<th><%= t('.body') %></th>
			<th colspan="3"><%= t('.actions_avail') %></th>
		</tr>
		<%= render @test.questions %>
	</table>
</div>
