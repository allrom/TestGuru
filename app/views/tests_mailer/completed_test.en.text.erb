                    * Fine! *
                   ----------
  Great! On <%= @sent_at %> You've completed the "<%= @test.title %>" test.
  Accomplish the rest: <%= @user.admin? ? admin_tests_url : tests_url %> .
